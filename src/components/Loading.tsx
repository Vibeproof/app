import { Center, Loader } from "@mantine/core";
import React from "react";


export default function Loading() {
    return (
        <Center mih={700}>
            <Loader />
        </Center>
    );
}
